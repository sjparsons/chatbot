node_modules/
dist/
build/
coverage/
*.log

# Incremental state from `tsc --build` (project references)
*.tsbuildinfo
.env
.env.local
.DS_Store
.vite/

# Claude Code — personal overrides and transient worktrees.
# .claude/settings.json is shared and tracked.
.claude/settings.local.json
.claude/worktrees/

# Local SQLite database and its WAL sidecars
data/
*.sqlite
*.sqlite-shm
*.sqlite-wal
