{
  "name": "chatbot",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n chat-api,chat-ui -c green,blue \"npm run dev -w @chatbot/chat-api\" \"npm run dev -w @chatbot/chat-ui\"",
    "build": "npm run build --workspaces --if-present",
    "start": "concurrently -n chat-api,chat-ui -c green,blue \"npm run start -w @chatbot/chat-api\" \"npm run start -w @chatbot/chat-ui\"",
    "test": "npm run test --workspaces --if-present",
    "eval": "npm run eval -w @chatbot/evals",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  }
}
